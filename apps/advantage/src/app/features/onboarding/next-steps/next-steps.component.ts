import { Component } from '@angular/core';
/**
 * Component that is used to render the next steps page
 * component used in the on boarding feature
 *
 *  Component declaration that is used to define:
 * - selector: used to define how to use the component in a template
 * - templateUrl: contains the html structure of the component
 * - styleUrls: contains the scss file used to style the component
 */
@Component({
    selector: 'ngx-next-steps',
    templateUrl: './next-steps.component.html',
    styleUrls: ['./next-steps.component.scss'],
    standalone: false,
})
/**
 * Class that creates the NextSteps component
 */
export class NextStepsComponent {}
