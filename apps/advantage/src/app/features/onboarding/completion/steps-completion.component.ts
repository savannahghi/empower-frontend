import { Component } from '@angular/core';
/**
 * Component that is used to render the onboarding completion page
 * component used in the on boarding feature
 *
 *  Component declaration that is used to define:
 * - selector: used to define how to use the component in a template
 * - templateUrl: contains the html structure of the component
 * - styleUrls: contains the scss file used to style the component
 */
@Component({
    selector: 'ngx-steps-completion',
    templateUrl: './steps-completion.component.html',
    styleUrls: ['./steps-completion.component.scss'],
    standalone: false,
})
/**
 * Class that creates the Onboarding Completion component
 */
export class StepsCompletionComponent {}
